#!/usr/bin/env bash

cd $(dirname $(readlink -f $0))

TRASH_CAN=saved
[ ! -d $TRASH_CAN ] && mkdir $TRASH_CAN 


for archive in $(find . -maxdepth 1 -type f -name "lab-crawl-*.tar.gz")
do
  tar xf $archive
  echo "Extracting results $archive"
  mv $archive $TRASH_CAN
done
